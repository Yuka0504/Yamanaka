<h1 class="section-title">ログイン</h2>

<%= form_with model: @user, url: user_session_path, class: 'sign-form' do |f| %>

  <div class="sign-content">
    <div class="sign-list">
      <div class="label">名前</div>
      <div><%= f.text_field :name, autofocus: true, autocomplete: "name", class: 'form-box' %></div>
    </div>
    <div class="sign-list">
      <div class="label">メールアドレス</div>
      <div><%= f.email_field :email, autocomplete: "email", class: 'form-box' %></div>
    </div>
    <div class="sign-list">
      <div class="label">パスワード</div>
      <div><%= f.password_field :password, autocomplete: "new-password", class: 'form-box' %></div>
    </div>
  </div>

  <div class="button">
    <%= f.submit "ログイン", class: 'button-link' %>
  </div>
<% end %>


